name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
        env:
          VITE_ADS_TOP_ID: ${{ secrets.VITE_ADS_TOP_ID }}
          VITE_ADS_SIDEBAR_ID: ${{ secrets.VITE_ADS_SIDEBAR_ID }}
          VITE_ADS_FOOTER_ID: ${{ secrets.VITE_ADS_FOOTER_ID }}
          # Note: GEMINI_API_KEY is handled client-side in this app via process.env.GEMINI_API_KEY
          # which is injected by AI Studio. For local/GitHub builds, users should set it.
      - name: Setup Pages
        uses: actions/configure-pages@v4
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
